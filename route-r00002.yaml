apiVersion: serving.knative.dev/v1alpha1
kind: Route
metadata:
  name: nodejs-runtime-example-module # The name of our route; appears in the URL to access the app
  namespace: default # The namespace we're working in; also appears in the URL to access the app
spec:
  traffic:
  - revisionName: nodejs-runtime-example-module-00001
    percent: 50
  - revisionName: nodejs-runtime-example-module-00002
    percent: 50
